# TEMPLATE VALUES - DO NOT USE IN PRODUCTION
# Run `make k8s-secrets` to generate cryptographically secure values,
# or manually replace the placeholders below before deploying.
apiVersion: v1
kind: Secret
metadata:
  name: sentinel-secrets
  namespace: sentinel-noc
  labels:
    app.kubernetes.io/part-of: sentinel-noc
type: Opaque
stringData:
  jwt-secret: "REPLACE_WITH_OUTPUT_OF__openssl_rand_base64_48"
  encryption-key: "REPLACE_WITH_OUTPUT_OF__openssl_rand_base64_32"
  mongo-username: "admin"
  mongo-password: "REPLACE_WITH_SECURE_RANDOM_PASSWORD"
